import React from 'react';

const ClientsPage = () => {
  return (
    <div style={{backgroundImage: `url("images/certs.jpg")`, height: "100vh", backgroundSize: "cover", backgroundPosition: "center"}}>
      {/* You can add content here if needed */}
    </div>
  );
}

export default ClientsPage;
